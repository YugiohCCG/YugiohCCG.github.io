# Python deps for the Omega CCG asset pipeline.
#
# Install once with:
#     python -m pip install -r scripts/requirements.txt
#
# All scripts target Python 3.11+ (tested on 3.14).

Pillow>=10.0
opencv-python>=4.8
numpy>=1.26
